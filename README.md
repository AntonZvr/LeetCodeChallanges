Problem Description
Given an integer array nums, rotate the array to the right by k steps.

Rotation to the right means the last k elements move to the front, and the remaining elements shift accordingly.
The rotation should be applied k times. If k is larger than the array's length, it effectively wraps around.
