Problem Description
The problem asks us to convert a given string s into a zigzag pattern on a given number of numRows. After filling out the pattern in a "Z" shape, we then read the characters row by row, from left to right, to form a new string.

Examples
Example 1:

s = "PAYPALISHIRING"
numRows = 3
Output: "PAHNAPLSIIGYIR"
